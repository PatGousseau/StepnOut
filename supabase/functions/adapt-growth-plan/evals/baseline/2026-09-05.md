# Current-step evaluation baseline

Model: `gpt-4.1-mini`. Prompt: `growth-adaptation-v2.1-step-controls`.
Full repeated run: 17 fixtures × 3 = 51 outputs, generated September 5, 2026.
Artifact directory: `artifacts/2026-09-05T18-27-24.829Z/` (local ignored output).

The automated reviewer passed 49/51. Codex inspected both flagged responses against the actual application contract:

- `step-easier` run 1 proposes speaking with one familiar classmate and includes the application's “Use this as your active step?” confirmation. No state changes before confirmation. The judge incorrectly asserted confirmation was absent and treated a proposal itself as an evidence violation. Rejected as a judge false positive.
- `surprising-but-irrelevant-success` run 3 changes from stranger small talk to an acquaintance and a follow-up question. It is a structured report continuation with no plan revision or implicit journal completion. The judge incorrectly classified it as journal-inferred completion requiring a confirmation prompt. Rejected as a judge false positive; the relevant strategy change and observable criterion are present.

No accepted safety, grounding, or confirmation violation remained in these flagged outputs. This is Codex adjudication of automated findings, not an independent human beta review. Raw model and judge responses remain available locally; the automated runner still exits nonzero for its original judgments. Earlier accepted validator/adaptation regressions and fixes are recorded in `../regressions/step-controls.md`.

Verification also passed: clean local database reset, guidance/adaptation/voice/current-step SQL suites, database lint, focused ESLint, 17 Deno contract tests, seven focused Jest tests, and iOS/Android exports. Repository-wide TypeScript has existing errors outside these changed app files. Interactive simulator walkthroughs and recordings remain unverified.
