# Regression cases

Add redacted failing evaluation artifacts here when they cause a prompt, model, schema, or safety
policy change. Convert stable failures into a fixture so later runs continue to exercise them.
