# Event selection baseline — 2026-09-05

Final configuration: `gpt-4.1`, `growth-events-v1.4`, temperature 0.25, eight synthetic scenarios repeated three times. All 24 schema/expected-selection checks passed. Codex inspected all 24 explanations and steps; this is not an independent human review or beta-readiness approval.

Local raw artifacts: `../artifacts/2026-09-05T18-53-25.706Z/` (ignored). The runner saves full inputs, raw responses, exact configuration, parsed outputs, and deterministic judgments. Reproduce with the README command; new model samples may differ.

| Scenario | Result, three runs each | Inspected reasoning |
| --- | --- | --- |
| Accessible small city | Select | Free, verified wheelchair access, Saturday timing, structured first introduction; 15/20-minute attendance or introduction criteria under user control. No verified recurrence asserted. |
| Professional meeting | No event | Actual workplace meeting fits; community practice violates the professional-only boundary. |
| Existing drawing class | No event | Confirmed recurring class and preference against another outing outweigh new inventory. |
| Timing mismatch | No event | Saturday event cannot fit weekday mornings. |
| Unknown wheelchair access | No event | Unknown access stays unknown; no invented accessible alternative venue. |
| Prior wrong-type rejection | No event | Production-shaped button feedback excludes materially similar conversation-table format. |
| Creative practice at home | No event | Social outing conflicts with private drawing goal and boundaries. |
| Group overchallenge | No event | Small facilitated groups still violate no-group boundary; no invented companion used to justify selection. |

Regression history is recorded in `../regressions/selection-grounding.md`. Selection-only success was insufficient: inspection caught unsupported ordinary-venue accessibility and recurrence even when event IDs were correct. The final context explicitly distinguishes a verified occurrence from unverified recurrence. One final explanation invites checking whether a setting could recur; it does not claim a recurring program exists.

Verification: clean `npm run db:reset`; event, step-controls, adaptation RLS, and voice-journal RLS SQL suites; `supabase db lint --local --level error`; 22 Deno tests; Deno checks of importer, endpoint, and evaluator; touched-app ESLint; 11 focused Jest tests; iOS and Android Expo exports. Local API smoke covers empty inventory, model-selected match, changed-venue rejection, confirmed single step, concurrent preference deletion, and account deletion.

Root `npx tsc --noEmit` remains failing on existing app errors and its inclusion of Deno files (including the new Deno importer/function). Changed app files have no reported errors; Deno's own checks pass. Interactive simulator flows/screenshots and approved live-source smoke are not verified here. No real source is activated or fixture deployed. The research harness in #275 remains untouched. These limitations remain draft-PR follow-up, not claims of completed beta readiness.
